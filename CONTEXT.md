# ユビキタス言語

| 用語 | 定義 |
|------|------|
| プロジェクト (Project) | タスクとカラムをまとめる最上位の単位。 |
| カラム (Column) | カンバンボード上の縦列。タスクのステータスを表す。 |
| タスク (Task) | 作業の最小単位。必ず1つのカラムに属する。 |
| 完了列 (Done Column) | プロジェクトごとに最大1つ指定できる特別なカラム。このカラムにタスクが移動すると「完了」として扱われる。 |
| 完了 (Done) | タスクが完了列に存在する状態。タスクが完了列に入った時刻が `completedAt` として記録される。完了列から出ると完了状態は解除される。 |
