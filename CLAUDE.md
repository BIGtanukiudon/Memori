## 要件

- 日本語で対応する。
- TDDで機能実装を進める。
- 機能の追加・修正があった場合、README.mdを追記・更新する。

## Agent skills

### Issue tracker

ローカル Markdown（`.scratch/` ディレクトリ）でイシューを管理。詳細は `docs/agents/issue-tracker.md` を参照。

### Triage labels

デフォルトのラベル語彙を使用。詳細は `docs/agents/triage-labels.md` を参照。

### Domain docs

シングルコンテキスト構成（ルートに `CONTEXT.md` + `docs/adr/`）。詳細は `docs/agents/domain.md` を参照。