# 08. アプリ内タイムラインビュー

Status: needs-triage
依存: 01〜03

## 目的

人が目視で振り返るためのビュー。CLI（issue 05）が日報用途を満たすので**後回し**。
CLI を実際に運用してみて、それでも欲しければ着手する。

## 想定スコープ

- 期間を選んでプロジェクト横断で作業ログを一覧表示
- 全文コピー（AI に貼る用）
- 既存の `AllTasksView` と並ぶ第3のビューとして追加

## 前提となる追加実装

issue 01 で見送った期間検索を TS 側にも追加する:

```ts
listWorkLogsBetween(db, fromUtcIso, toUtcIso, projectId?)
```

- `LEFT JOIN tasks` で現在名を解決し、削除済みフラグを返す
- ローカル日付 → UTC 半開区間の変換は `src/lib/date.ts` に追加する
  （issue 04 の Rust 実装と**同じロジックの二重実装**になる点に注意。
  境界値テストも同様に必要）

## 判断が必要な点

- そもそも必要か（CLI で足りている可能性）
- 必要なら、期間プリセット（今日／今週／今月）をどこまで用意するか
